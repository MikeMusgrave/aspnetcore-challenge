@model IEnumerable<FoodItem>

@{ 
    Layout = null;
}

<div>
    <table>
        @foreach (FoodItem f in Model)
        {
            <tr>
                <td><a class="btn btn-link" asp-action="Detail" asp-route-id="@f.Id">@f.Name</a></td>
            </tr>
        }
    </table>
</div>
